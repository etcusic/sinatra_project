<%# SHOW A DIFFERENT VIEW IF SESSION_ID DOES NOT MATCH USERS/:ID %>

<img class="logo" src="<%= @user.photo_url %>" alt="user photo">
<h1>Welcome to your homepage <%= @user.name %>!</h1>
<br>

<h2><%= @user.name %> teams:</h2>
<% @user_teams.each do |team| %>
    <a href="/teams/<%= team.id %>"><h3><%= team.location %> <%= team.name %></h3></a>
<% end %>
<br><br>

<h3>Create new team here:</h3> 
<a href="/teams/new"><button>Create</button></a>

<h3>-- OR --</h3>
<a href="/users/<%= @user.id %>/edit"><button>Edit Info</button></a>

<h3>-- OR --</h3>
<a href="/logout"><button>Logout</button></a>
<br><br>

<%# User.find(2).photo_url = "/seed_user_photos/jenny.jpg" %>